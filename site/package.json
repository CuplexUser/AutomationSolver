{
  "name": "@automationsolver/site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The GitHub Pages landing page. Its playable rung imports the shared engine, so the demo and the game grade identically.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@automationsolver/shared": "*"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
